meta:
  environment: nodejs
  port: 3000
  toolchain:
    name: npm
    version: 20
run:
  persistence:
    - mountPath: /app/prisma-data
      name: data
  command: mkdir -p /app/prisma-data && npx prisma generate && npm run build && npx prisma db push && npm run start
