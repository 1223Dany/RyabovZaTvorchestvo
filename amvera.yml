meta:
  environment: nodejs
  port: 3000
  toolchain:
    name: npm
    version: 20
run:
  persistence:
    - mountPath: /app/prisma-data
      name: data
  command: npx prisma db push && npm run start
